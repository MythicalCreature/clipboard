---
date: 2026-05-21T23:06:02+08:00
source: clipboard
chars: 454
---

// 生成 suites 数组
suites = suiteList.map((item, idx) => ({
    suiteId: idx + 1,
    suiteName: item.name,
    count: item.questions.length,
    questions: item.questions // 👈 必须加这一句！！！
}));
console.log("✅ suites 生成完成：", suites);

// ======================================
// 👇 就放在这里！！！
// ======================================
initSuites();
render();

console.log("=== 全部加载完成 ===");

---
date: 2026-05-22T00:21:47+08:00
source: clipboard
chars: 587
---

// 自动提取 年份
const allYears = [...new Set(suites.map(s => {
  const m = s.name.match(/\d{4}/);
  return m ? m[0] : null;
}).filter(Boolean))].sort((a, b) => b - a);

// 自动提取 大分类（如：言语理解与表达、中级...）
const allCategories = [...new Set(suites.map(s => {
  if (s.name.includes("言语理解与表达")) return "言语理解与表达";
  if (s.name.includes("中级")) return "中级";
  return null;
}).filter(Boolean))];

// 自动提取 地区/试卷类型（严格按你规则）
const allAreas = ["广东", "联考"]; // 固定筛选选项

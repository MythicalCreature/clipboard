---
date: 2026-05-22T20:03:30+08:00
source: clipboard
chars: 504
---

// 获取所有图片URL
const imgs = Array.from(document.querySelectorAll('img'))
  .map(img => img.src || img.currentSrc)
  .filter(src => src.includes('modao.cc') && src.endsWith('.png'));

// 批量下载
imgs.forEach((url, index) => {
  setTimeout(() => {
    const a = document.createElement('a');
    a.href = url;
    a.download = `modao_img_${index}.png`;
    document.body.appendChild(a);
    a.click();
    document.body.removeChild(a);
  }, index * 200); // 加延时避免浏览器拦截
});

---
date: 2026-05-22T00:09:59+08:00
source: clipboard
chars: 747
---


// 按 p 标签分段 + 只清理 HTML 标签 + 保留所有符号
function parseHtmlWithParagraphs(html) {
    if (!html) return "";
console.log(html);
    // 1. 先处理下划线
    html = cleanUnderlineSpaces(html);

    // 2. 按 </p> 分割成段落
    const paragraphs = html.split(/<\/p>/i).map(p => {
        return p.replace(/<p\s*\/?>/gi, "")    // 去掉 <p>
            .replace(/<[^>]+>/g, "")       // 去掉所有其他标签
            .replace(/&nbsp;/g, " ")       // 空格转普通空格
            .replace(/\s+/g, " ")          // 多空格合并
            .trim();
    }).filter(line => line); // 过滤空行
console.log(paragraphs);
    // 3. 段落之间用换行符 \n 连接
    return paragraphs.join('\n');
}


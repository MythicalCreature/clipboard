---
date: 2026-05-24T17:25:08+08:00
source: clipboard
chars: 480
---

// ======================
// ✅ 【核心新增】每道题请求考点
// ======================
console.log(`⏳ 正在获取第 ${num} 题考点，ID: ${questionId}`);
const keyPointText = await getQuestionKeyPoints(questionId);
console.log(`✅ 第 ${num} 题考点：${keyPointText}`);

// 写入考点（你要的格式）
children.push(new Paragraph({
  children: [
    new TextRun({
      text: `考点：${keyPointText}`,
      bold: true,
      size: 24
    })
  ]
}));
